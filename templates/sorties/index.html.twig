{% extends 'base.html.twig' %}

{% block title %}Sorties index{% endblock %}

{% block body %}
    <h1 class="text-center">Liste des sorties</h1>
    <br>
    <a class="btn btn-outline-info btn-sm" href="{{ path('app_sorties_new') }}">Ajout</a>
    <br>
    <br>

<h2>Filtrer les sorties</h2>

    {{  form(form_filtre) }}

<br>
    <table class="table table-striped table-sm table-bordered">
        <thead class="thead-dark">
            <tr>
                <th>N° sortie</th>
                <th>Nom</th>
                <th>Debut de la sortie</th>
                <th>Duree en minutes</th>
                <th>Date limite d'inscription</th>
                <th>Inscrits / Places</th>
                <th>Description de la sortie</th>
                <th>Etat de la sortie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sortie in sorties %}
            <tr>
                <td><a class="btn btn-outline-info btn-sm" href="{{ path('app_sorties_show', {'id': sortie.id}) }}">{{ sortie.id }}</td>
                <td>{{ sortie.nom }}</td>
                <td>{{ sortie.dateDebut ? sortie.dateDebut|date('d/m/Y H:i:s') : '' }}</td>
                <td>{{ sortie.duree }}</td>
                <td>{{ sortie.dateCloture ? sortie.dateCloture|date('d/m/Y') : '' }}</td>
                <td>{{ sortie.users|length }} / {{ sortie.nbInscriptionsMax }}</td>
                <td>{{ sortie.descriptionInfos }}</td>
                <td>{{ sortie.etat.libelle }}</td>
                <td>
                    <a class="btn btn-outline-info btn-sm" href="{{ path('app_sorties_edit', {'id': sortie.id}) }}">MAJ </a>
                    {{ include('sorties/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">Pas de sortie</td>
            </tr>
        {% endfor %}

        </tbody>

    </table>
    {% if page > 1 %}
        <a href="{{ path('app_sorties_index', {'page' : page - 1}) }}"><img class="gif2"  src="{{  asset('images/pageprecedente.gif') }}"></a>
    {% endif %}&nbsp;
    {% if page < nbPagesMax %}
        <a href="{{ path('app_sorties_index', {'page' : page + 1}) }}"><img class="gif2"  src="{{  asset('images/pagesuivante.gif') }}"></a>
    {% endif %}
   <br>

{% endblock %}
